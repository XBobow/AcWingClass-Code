#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int main() {

    double a, b, c;
    cin >> a >> b >> c;

    printf("MEDIA = %.1lf", (2 * a + 3 * b + 5 * c) / 10);

    return 0;
}