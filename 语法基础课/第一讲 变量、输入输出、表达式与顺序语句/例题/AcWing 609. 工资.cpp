#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int main() {

    int a, b;
    float c;

    cin >> a >> b >> c;

    printf("NUMBER = %d\nSALARY = U$ %.2f", a, b * c);

    return 0;
}