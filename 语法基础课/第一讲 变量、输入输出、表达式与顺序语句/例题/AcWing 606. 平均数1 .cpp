#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int main() {

    float a, b;
    cin >> a >> b;

    printf("MEDIA = %.5f", (3.5 * a + 7.5 * b) / 11);

    return 0;
}