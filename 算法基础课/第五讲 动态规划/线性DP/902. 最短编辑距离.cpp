#include <bits/stdc++.h>
#include <cstdio>
//#include <iostream>
using namespace std;

const int N = 1010;

int n, m;
char a[N], B[N];
int f[N];

int main() {

    scanf("%d", &n);
    scanf("%s", a + 1);
    scanf("%d", &m);
    scanf("%s", b + 1);

    for (int i = 1; i <= n; i++)
        for (int j = 1; j <= m; j++) {

        }

    return 0;
}